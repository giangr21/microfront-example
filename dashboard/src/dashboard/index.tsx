const Dashboard: React.FC = () => {
  return (
    <>
      <h1>Dashboard</h1>
    </>
  );
};

export { Dashboard };

// import Parcel from "single-spa-react/parcel";
{
  /* <Parcel config={() => System.import("@nitro/parcel")} /> */
}
// CALL A EVENT
// const testParcel = () => {
//   dispatchEvent(new CustomEvent("@test-event", { detail: { id: "test" } }));
// };

// (NECESSARY TO LISTEN TO IT ON THE OTHER SIDE)
// window.addEventListener("@nitro/parcel", (event) => {do smth})
